import 'package:flutter/material.dart';

void main(List<String> args) {
  runApp(IOFlutter());
}

class IOFlutter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(),
    );
  }
}
